package lists;

import java.util.*;

public class ListDemo {
    public static void main(String[] args) {
        // an order collection of elements, allows duplicate elements
        // allows position access of elements

        List arrayList = new ArrayList();
        List linkedList = new LinkedList();
        List vector = new Stack();
        List stack = new Stack();

        // or
    }



}
